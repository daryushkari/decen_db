localdb query database_name insert collection_name {...}
    insert document with values in {...} into collection

localdb query database_name find collection_name where {condition}
    show documents which satisfy condition

localdb query database_name update collection_name where {condition} with {new values}
    update all documents with new values

localdb query database_name delete collection_name where {condition}
    delete documents with condition
